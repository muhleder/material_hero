name: card_hero
description: Container transform animation, based upon local_hero
version: 0.1.0
homepage: https://github.com/muhleder/card_hero

environment:
  sdk: '>=2.18.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
